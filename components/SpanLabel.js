export default function SpanLabel({ children }) {
  return (
    <span className="font-eudoxus tracking-tight bg-blue-100 font-semibold px-4 py-1 rounded-full text-blue-600 text-sm">
      {children}
    </span>
  );
}
